version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd frontend2
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: frontend2/dist
    files:
      - '**/*'
  cache:
    paths:
      - frontend2/node_modules/**/*
